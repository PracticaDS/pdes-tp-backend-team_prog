import request from 'supertest'
import app from '../server'

import NodeCtrl from './nodeController'

describe('CREATE node', () => {

  it('requesting /api gives a status ok', () => {
    NodeCtrl.
  })

})
